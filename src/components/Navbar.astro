<nav
  class="relative z-50 w-full border-b border-gray-200 bg-white/50 backdrop-blur-sm dark:border-gray-500 dark:bg-gray-900/50"
>
  <div
    class="mx-auto flex max-w-5xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8"
  >
    <!-- Logo -->
    <div class="shrink-0">
      <a
        href="/"
        class="text-xl font-bold text-gray-900 hover:text-blue-600 dark:text-white dark:hover:text-blue-400"
      >
        Eza.
      </a>
    </div>

    <!-- Mobile Menu Button (Hamburger) -->
    <button
      id="mobile-menu-btn"
      class="p-2 text-gray-600 hover:text-blue-600 md:hidden dark:text-gray-300 dark:hover:text-white"
      aria-label="Toggle menu"
    >
      <!-- Icon Menu (SVG) -->
      <svg
        id="menu-icon"
        class="h-6 w-6"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
      <!-- Icon Close (SVG) - Hidden by default -->
      <svg
        id="close-icon"
        class="hidden h-6 w-6"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M6 18L18 6M6 6l12 12"></path>
      </svg>
    </button>

    <!-- Menu Links -->
    <div
      id="mobile-menu"
      class="absolute top-full left-0 hidden w-full border-b border-gray-200 bg-white/95 backdrop-blur-md md:static md:block md:w-auto md:border-none md:bg-transparent dark:border-gray-500 dark:bg-gray-900/95"
    >
      <ul
        class="flex flex-col gap-6 p-6 text-center md:flex-row md:items-center md:gap-8 md:p-0"
      >
        <li>
          <a
            href="/"
            class="text-sm font-medium text-gray-600 transition-colors hover:text-blue-600 dark:text-gray-300 dark:hover:text-white"
            aria-current="page"
          >
            About
          </a>
        </li>
        <li>
          <a
            href="/projects"
            class="text-sm font-medium text-gray-600 transition-colors hover:text-blue-600 dark:text-gray-300 dark:hover:text-white"
          >
            Projects
          </a>
        </li>
        <li>
          <a
            href="/blog"
            class="text-sm font-medium text-gray-600 transition-colors hover:text-blue-600 dark:text-gray-300 dark:hover:text-white"
          >
            Blog
          </a>
        </li>
        <li>
          <a
            href="/til"
            class="text-sm font-medium text-gray-600 transition-colors hover:text-blue-600 dark:text-gray-300 dark:hover:text-white"
          >
            TIL
          </a>
        </li>
        <li>
          <a
            href="/contact"
            class="text-sm font-medium text-gray-600 transition-colors hover:text-blue-600 dark:text-gray-300 dark:hover:text-white"
          >
            Contact
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<!-- Script untuk Toggle Menu (Astro Client Side) -->
<script>
  const btn = document.getElementById("mobile-menu-btn");
  const menu = document.getElementById("mobile-menu");
  const menuIcon = document.getElementById("menu-icon");
  const closeIcon = document.getElementById("close-icon");

  btn?.addEventListener("click", () => {
    menu?.classList.toggle("hidden");
    menuIcon?.classList.toggle("hidden");
    closeIcon?.classList.toggle("hidden");
  });
</script>
